using UnityEngine;

namespace Core.Modules.GameOverScreen.Scripts.Presenters
{
    public class GameOverScreenPresenter : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
